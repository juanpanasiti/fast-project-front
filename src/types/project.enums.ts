export enum ProjectStatus {
    NEW = 'new',
    IN_PROGRESS = 'in-progress',
    ON_HOLD = 'on-hold',
    FINISHED = 'finished',
    CANCELLED = 'cancelled',
}

export enum ProjectPriority {
    LOW = 'low',
    MEDIUM = 'medium',
    HIGH = 'high',
}
